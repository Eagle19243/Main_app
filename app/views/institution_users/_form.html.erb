<%= simple_form_for(@institution_user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :institution %>
    <%= f.association :user %>
    <%= f.input :position %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
