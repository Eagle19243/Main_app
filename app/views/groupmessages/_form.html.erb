<%= simple_form_for(@groupmessage) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :messgae %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
