<div class="landing-header">
  <div class="header-wrap">
    <div class="landing-logo">
      <% if user_signed_in? %>
          <!-- For signed users, we should still this to redirect to root path as we have only dummy projects -->
          <%= link_to root_path do %>
            WeServe.io
            <small>Beta</small>
          <% end %>
      <% else %>
          <%= link_to root_path do %>
            WeServe.io
            <small>Beta</small>
          <% end %>
      <% end %>
    </div>

    <a class="header-link border-link ml30 hidden-mobile" data-modal="#projectSignupModal" id="start_project_link">Start a Project</a>
    <div class="header-dropdown">
        <div class="header-dropdown__title js-dropdown">Explore</div>
        <ul class="header-dropdown__list js-dropdown-list">
            <li class="header-dropdown__item">
                <a class="projects-link" href="<%= projects_path %>">Active Projects</a>
            </li>
            <li class="header-dropdown__item">
                <a href="<%= ENV['about_project_url'] %>">About</a>
            </li>
            <li class="header-dropdown__item">
                <a href="<%= t('commons.knowledge_base_landing_link') %>">Improve the Platform</a>
            </li>
            <li class="header-dropdown__item">
                <a href="<%= t('commons.faq_landing_link') %>">FAQ</a>
            </li>
        </ul>
    </div>
    <!--<%= link_to 'Active Projects', projects_path, data: {no_turbolink: true}, :class => "projects-link hidden-mobile" %>-->

    <div class="pull-right register-group">
      <a data-modal="#registerModal" class="show-desktop sign_in_a">Login</a>
      <span class="show-desktop">or</span>
      <a data-modal="#registerModal" class="border-link show-desktop sign_up_a">Register</a>
      <a href="javascript:void(0)" class="btn-mobile hamburger show-tablet text-right">
        <i class="fa fa-bars" aria-hidden="true"></i>
      </a>
      <a href="javascript:void(0)" class="btn-mobile cross text-center">
        <i class="fa fa-times" aria-hidden="true"></i>
      </a>
    </div>
  </div>
</div>

<div class="mobile-menu display-none">
  <ul class="mobile-menu-list">
    <li class="border-bottom show-mobile">
      <a data-modal="#projectSignupModal" id="start_project_link">Start a Project</a>
    </li>
    <li class="border-bottom show-mobile">
      <%= link_to 'Active Projects', projects_path, data: {no_turbolink: true} %>
    </li>
    <li class="border-bottom">
      <a data-modal="#registerModal" class="sign_in_a">Login</a>
    </li>
    <li>
      <a class="sign_up_a" data-modal="#registerModal">Register</a>
    </li>
  </ul>
</div>

<script>

$(document).ready(function() {
    var $dropdown = $('.js-dropdown');

    $(document)
        .on('click.dropdownToggle', '.js-dropdown', function() {
            $(this).toggleClass('_active');
            $(this).siblings('.js-dropdown-list').toggleClass('_active');
        })
        .on('click.hideDropdown', function(e) {
            if (!$(e.target).closest('.js-dropdown-list').length && !$(e.target).closest('.js-dropdown').length) {
                hideDropdowns();
            }
        })

    $(window).on('resize', function() {
        hideDropdowns();
    })

    function hideDropdowns() {
        $dropdown.removeClass('_active');
        $('.js-dropdown-list').removeClass('_active');
    }
})

</script>