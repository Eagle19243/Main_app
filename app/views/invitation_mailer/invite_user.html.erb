<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" content="text/html" http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      <bold><%= t('.body', user_name: @user_name) %> </bold>
      <a href="<%=task_url(id: @task.id, taskId: @task.id, tab: 'tasks' )  %>"> <%= @task.title %> </a>
    </p>
  </body>
</html>
